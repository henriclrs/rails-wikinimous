<div class="container">
  <% @articles.each do |article| %>
    <h2 class="text-center">
      <%= link_to article[:title], article_path(article) %>
    </h2>
    <p class="text-center">
      <%= article[:content] %>
    </p>
  <% end %>
</div>

